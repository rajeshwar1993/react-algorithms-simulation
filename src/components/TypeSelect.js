import React from "react";

const TypeSelect = props => {
  return (
    <div className={"one-row"}>
      <div className={"type-select"}>Type Select</div>
    </div>
  );
};

export default TypeSelect;
